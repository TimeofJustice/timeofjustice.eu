/**
 * The keyboard-focus ring, shared by every interactive component so focus does
 * not change shape as it moves. `focus-visible` keeps it off the pointer.
 */
export const FOCUS_RING =
  "focus-visible:outline-none focus-visible:shadow-[0_0_0_0.2rem_var(--color-focus-ring)]";
